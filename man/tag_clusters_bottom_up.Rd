% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tagging.R
\name{tag_clusters_bottom_up}
\alias{tag_clusters_bottom_up}
\title{Tag clusters from the bottom of the hierarchy upward}
\usage{
tag_clusters_bottom_up(state, sample_size, progress_path, model, base_url)
}
\arguments{
\item{sample_size}{Number of example questions to include in the prompt.}

\item{progress_path}{File path where the full tagging state list is stored.}

\item{model}{Ollama chat model for tagging.}

\item{clusters}{Tibble produced by \code{\link[=build_cluster_index]{build_cluster_index()}}.}

\item{questions}{Question tibble with \code{id} and \code{caption} columns.}

\item{assignments}{Question-to-cluster assignments. Persisted so an interrupted
run can resume with full context.}

\item{base}{Base URL for the Ollama server.}
}
\value{
The clusters tibble with the \code{tag} column filled.
}
\description{
Iterates through each level, generating tags for clusters whose tags are not
yet set. After each successful tag, the updated state is written to
\code{progress_path}. The hierarchy is tagged bottom up meaning the leaf nodes
are tagged first. Thereafter parent nodes are tagged with each tag getting broader
to describe a bigger selection of questions.
}
\keyword{internal}
