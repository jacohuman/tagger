% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{add_cluster_assignments}
\alias{add_cluster_assignments}
\title{Add cluster assignments to question table}
\usage{
add_cluster_assignments(questions, hclust, clusters_by_level)
}
\arguments{
\item{questions}{Tibble with question data.}

\item{hclust}{Hierarchical clustering object.}

\item{clusters_by_level}{Integer vector of cluster counts by level.}
}
\value{
Tibble with cluster_level_* columns.
}
\description{
Add cluster assignments to question table
}
\examples{
questions <- tibble::tibble(id = c("q_001", "q_002"), caption = c("Age?", "Income?"))
embeddings <- matrix(rnorm(20), ncol = 10)
tree <- stats::hclust(stats::dist(embeddings))
add_cluster_assignments(questions, tree, clusters_by_level = c(2))
}
