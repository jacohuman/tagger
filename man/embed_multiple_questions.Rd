% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{embed_multiple_questions}
\alias{embed_multiple_questions}
\title{Embed multiple question texts}
\usage{
embed_multiple_questions(texts, model, base_url, timeout_sec = 60)
}
\arguments{
\item{texts}{Character vector. Each element is a question to embed.}

\item{model}{Embedding model name or tag available on the running Ollama instance.}

\item{base_url}{Base URL for the Ollama server.}

\item{timeout_sec}{Request timeout in seconds.}
}
\value{
A list of numeric vectors. Each list element is the embedding for
the corresponding element of \code{texts}.
}
\description{
Wrapper that iterates over a character vector of texts and
calls \code{\link[=embed_single_question]{embed_single_question()}} for each element. As the texts are sent to the
embedding model a simple textual progress bar is displayed.
}
\examples{
\dontrun{
qs <- c("What is your age?", "What is your highest level of education?")
embs <- embed_multiple_questions(
  qs,
  model = "nomic-embed-text",
  base_url = "http://localhost:11434"
)
str(embs)
}

}
\seealso{
\code{\link[=embed_single_question]{embed_single_question()}}
}
