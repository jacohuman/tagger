% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_report}
\alias{audit_report}
\title{Build a tag audit report (plural removal, monotonicity, embeddings)}
\usage{
audit_report(
  audit_df,
  caption_emb,
  tag_emb,
  path_col = "after_path",
  min_freq_for_syn = 2,
  min_path_sim = 0.25,
  min_tag_sim_threshold = 0.1
)
}
\arguments{
\item{audit_df}{A tibble containing at least \code{id}, \code{caption}, and \code{after_path}.}

\item{caption_emb}{Named list of caption embedding vectors keyed by \code{id}, or list-column \code{caption_emb} in \code{audit_df}.}

\item{tag_emb}{Named list mapping canonical tag strings to embedding vectors.}

\item{path_col}{Name of the path column (default \code{"after_path"}).}

\item{min_freq_for_syn}{Minimum tag frequency for JW synonym candidate generation (default 2).}

\item{min_path_sim}{Threshold for caption vs mean(path) similarity (default 0.25).}

\item{min_tag_sim_threshold}{Threshold for worst tag similarity (default 0.10).}
}
\value{
A named list containing:
\itemize{
\item \code{tag_long}: long-form tag table (tags canonicalized + singularized)
\item \code{tag_registry}: tag frequency registry
\item \code{synonyms_high}: high-confidence JW merge candidates
\item \code{synonyms_uncertain}: uncertain JW candidates
\item \code{hierarchy_violations}: monotonicity violations
\item \code{caption_mismatches}: embedding-based mismatch shortlist
}
}
\description{
Runs an audit over cleaned paths with:
\enumerate{
\item plural removal via \code{\link{canon_tag}} (applied when parsing paths)
\item generality check using support monotonicity (\code{\link{audit_hierarchy}})
\item embedding-based caption-to-path sanity check (\code{\link{audit_caption_fit_embeddings}})
}

Also includes string-based near-duplicate candidates (Jaro-Winkler) as a deterministic
first pass for synonym-like merges.
}
