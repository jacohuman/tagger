% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{unnest_questions}
\alias{unnest_questions}
\title{Extract unique question captions from nested forms}
\usage{
unnest_questions(forms, limit_n = Inf)
}
\arguments{
\item{forms}{A data frame (or tibble) with a list-column named \code{form}
containing data frames/tibbles that include a \code{caption} column for each question.}

\item{limit_n}{Limit the number of question captions to select for tagging.  This is
used during development to reduce the waiting time between pipeline runs.}
}
\value{
A tibble with two columns:
\itemize{
\item \code{caption} — the cleaned, unique caption text
\item \code{id} — a zero-padded identifier in the form \code{"q_###"}
}
}
\description{
Flattens a nested \code{form} column, pulls the \code{caption} field of the questions,
normalizes white space, removes empty or \code{NA} captions and returns
the first \code{limit_n} rows with a generated ID of the form \code{"q_001"},
\code{"q_002"}, ...
}
\examples{
forms <- tibble::tibble(
  form = list(tibble::tibble(caption = c("Age", "Household size", "Age")))
)
unique_q <- unnest_questions(forms, limit_n = 100)

}
