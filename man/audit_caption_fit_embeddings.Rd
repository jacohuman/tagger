% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_caption_fit_embeddings}
\alias{audit_caption_fit_embeddings}
\title{Audit whether a question's tag path makes sense using embeddings}
\usage{
audit_caption_fit_embeddings(
  audit_df,
  caption_emb,
  tag_emb,
  path_col = "after_path",
  min_path_sim = 0.25,
  min_tag_sim_threshold = 0.1,
  top_n_limit = NULL
)
}
\arguments{
\item{audit_df}{Tibble with \code{id}, \code{caption}, and a path column (default \code{"after_path"}).}

\item{caption_emb}{Named list of numeric vectors keyed by \code{id}, or a list-column \code{caption_emb} in \code{audit_df}.}

\item{tag_emb}{Named list mapping canonical tag strings to numeric vectors.}

\item{path_col}{Name of the path column (default \code{"after_path"}).}

\item{min_path_sim}{Flag if caption vs mean(path) cosine similarity is below this (default 0.25).}

\item{min_tag_sim_threshold}{Flag if worst (minimum) tag similarity is below this (default 0.10).}

\item{top_n_limit}{Optional integer; if set, return only the worst N flagged rows.}
}
\value{
A tibble of flagged rows with similarity diagnostics and the worst offending tag.
}
\description{
Flags suspicious questions by comparing each caption embedding to:
\itemize{
\item the mean embedding of the tag path (overall coherence)
\item each individual tag embedding (worst offending tag)
}
}
